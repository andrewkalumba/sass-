export type ContentArticleProps = {
    title: string
    image: string
    style: string
}

export interface PokemonType {
    id?:number
    name:string
    image:string
    types:string[]
}

export type typesItemProp = {
    name: string;
    url: string;
}
